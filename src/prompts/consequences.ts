// Phase 3 will fill these in with the full system prompt + few-shots from spec §7.
// Stubbed so engine/stream.ts compiles cleanly during Phase 1.

export const CONSEQUENCES_SYSTEM = '';
export const COMPOUND_SYSTEM = '';
export const userMessage = (decision: string) => decision;
export const ASSISTANT_PREFILL = '{';
